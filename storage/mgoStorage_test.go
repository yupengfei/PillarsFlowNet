package storage

import (
	"testing"
)

func TestStoreToChart(t * testing.T) {
	// from_user_code := "123"
	// to_user_code := "456"
	// message := "test"
	// receipt := false
	// StoreToChart(&from_user_code, &to_user_code, receipt, nil)
}